<div class="message" id="message-<%= message.id %>">
  <p><%= message.id %>: <%= message.user.username %>: <%= l message.created_at, format: "%-d日(%a) %H:%M" %></p>
  <% if message.images? %>
    <%#= image_tag message.image.url %>
    <% message.images.each do |image| %>
      <%= image_tag image.url %>
    <% end %>
  <% end %>
  <%= simple_format(h message.content) %>
</div>