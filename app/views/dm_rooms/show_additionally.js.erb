var messageHeight = dmMessageContainer.scrollHeight
messageContainer.insertAdjacentHTML('afterbegin', "<%= j(render @dm_messages) %>")
var newMessageHeight = dmMessageContainer.scrollHeight
window.scrollBy(0, newMessageHeight - messageHeight)
<!--メッセージが存在する時だけさらに読み込み可能とする-->
<% if @dm_messages.present? %>
  showAdditionally = true
<% end %>