<div id="dm_message-container">
  <% if @dm_messages.present? %>
    <%= render @dm_messages %>
  <% else %>
    <h3 class="text-center">メッセージはまだありません</h3>
  <% end %>
</div>



<%= render "dm_footer" %>